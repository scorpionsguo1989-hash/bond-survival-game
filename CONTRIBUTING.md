# 贡献指南

感谢你愿意改进债市生存游戏。

## 开发环境

- Node.js 22（仓库根目录提供 `.nvmrc`）
- npm 10 或兼容版本

安装依赖并运行测试：

```bash
npm ci
npm ci --prefix api
npm test
```

启动静态页面：

```bash
npm run serve
```

然后访问 `http://localhost:8080`。

## 提交修改

1. 将功能或修复控制在清晰、可复核的范围内。
2. 涉及规则、计分或角色逻辑时，同步增加或更新测试。
3. 提交前运行完整测试，并说明仍未覆盖的场景。
4. 不要提交真实玩家数据库、API 密钥、`.env` 文件或含凭据的部署配置。
5. 涉及政策、市场事件或历史案例时，说明资料来源和适用时间，不把游戏设定表述为现实业务结论。

## 内容边界

本项目用于教育、研究和情景模拟。贡献内容不得包含客户资料、未公开项目信息、个人敏感信息或无法公开授权的素材。
